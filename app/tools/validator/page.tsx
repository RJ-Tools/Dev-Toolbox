import { ValidatorViewer } from "@/components/tools/validator-viewer"

export const metadata = {
    title: "Validator - Developer Tools",
    description: "Validate JSON, YAML, XML",
}

export default function ValidatorPage() {
    return <ValidatorViewer />
}
